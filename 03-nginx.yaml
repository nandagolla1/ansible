- name: install nginx server
  hosts: frontend
  tasks:
    - name: install nginx server
      ansible.builtin.debug:
        msg: "installing nginx"

    - name: install nginx 
      ansible.builtin.package:
        name: nginx
        state: present
